
# This file was generated. Do not edit manually.

schema {
  query: Query
}

"Indicates exactly one field must be supplied and this field must not be `null`."
directive @oneOf on INPUT_OBJECT

type DateTime {
  gmt: String!
  iso: String!
  pretty: String!
}

type Profile {
  avatarUrl: String
  createdAt: String!
  firstName: String
  id: ID!
  lastName: String
  telegramId: Int
  updatedAt: String!
  userId: String!
}

type Query {
  hello: String!
  manyVoting(isActive: Boolean): [Voting!]!
  oneVoting(id: ID!): Voting
  user(id: ID!): Test!
}

type Test {
  email: String!
  id: ID!
  name: String!
}

type User {
  createdAt: String!
  email: String
  id: ID!
  login: String!
  password: String
  profile: Profile
  updatedAt: String!
}

type Voting {
  author: User!
  authorId: String!
  choices: [VotingChoice!]
  createdAt: String!
  date: DateTime!
  description: String
  finishIn: String
  id: ID!
  isActive: Boolean!
  title: String!
  updatedAt: String!
  votesNumber: Int!
}

type VotingChoice {
  count: Int!
  createdAt: String!
  id: ID!
  label: String!
  updatedAt: String!
  value: String!
  votingId: ID!
}

enum AllowedColor {
  BLUE
  GREEN
  RED
}
